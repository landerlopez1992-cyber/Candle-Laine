/** Row `user_addresses` (Supabase). */
export type UserAddressRow = {
  id: string;
  user_id: string;
  label: string;
  country_iso2: string;
  state_code: string;
  municipality: string;
  street: string;
  zip: string;
  created_at: string;
  updated_at: string;
};
